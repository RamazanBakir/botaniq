// This layout is required by Next.js but the actual layout
// is handled by app/[locale]/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
